name: Apply Finanza updates
on:
  workflow_dispatch:
  push:
    paths:
      - '.github/scripts/apply_finanza_updates.py'
      - '.github/workflows/apply-finanza-updates.yml'
      - 'FINANZA_UPDATE_TRIGGER.txt'
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply requested UI updates
        run: |
          python .github/scripts/apply_finanza_updates.py
          python - <<'PY'
          from pathlib import Path
          p=Path('index.html'); s=p.read_text(encoding='utf-8')
          s=s.replace('<script id="finanza-requested-js">','')
          s=s.replace('</script>\n</script></body></html>','</script></body></html>',1)
          if 'function budgetMoney(' not in s:
              marker='/* Histórico de presupuesto:'
              s=s.replace(marker,"function budgetMoney(n,c){try{return new Intl.NumberFormat(db?.language||'es',{minimumFractionDigits:2,maximumFractionDigits:2}).format(Number(n)||0)}catch{return (Number(n)||0).toFixed(2)}}\\n"+marker,1)
          p.write_text(s,encoding='utf-8')
          PY
          sed -i "s/if(user\\&\\&db\\&\\&loginMode!=='new')/if(user\\&\\&db)/" index.html
      - name: Commit updated app
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html
          git diff --cached --quiet || git commit -m 'Mejoras de presupuesto, gráficos y moneda de registro'
          git push
      - name: Validate
        run: |
          test -s index.html
          test "$(grep -o 'id=\"finanza-requested-fixes\"' index.html | wc -l)" -eq 1
          grep -q 'function budgetMoney' index.html
          grep -q 'function renderBudgetVsSpentChart' index.html
          grep -q 'function renderDebtChart' index.html
          grep -q 'Moneda de preferencia' index.html
          grep -q 'Las cuotas y responsabilidades se gestionan desde Pagos' index.html
