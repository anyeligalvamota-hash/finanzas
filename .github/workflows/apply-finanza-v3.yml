name: Apply Finanza V4 design
on:
  push:
    paths:
      - '.github/scripts/apply_finanza_v3.py'
      - '.github/workflows/apply-finanza-v3.yml'
      - 'FINANZA_V3_TRIGGER.txt'
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply Finanza V4 improvements
        run: python .github/scripts/apply_finanza_v3.py
      - name: Validate HTML and JavaScript
        run: |
          test -s index.html
          test "$(grep -o 'id=\"finanza-v4-css\"' index.html | wc -l)" -eq 1
          test "$(grep -o 'id=\"finanza-v4-js\"' index.html | wc -l)" -eq 1
          grep -q 'Presupuestado' index.html
          grep -q 'Responsabilidades de pago' index.html
          grep -q 'finanzaEditPaymentV4' index.html
          grep -q 'finanzaDeletePaymentV4' index.html
          grep -q 'Paleta y tipografía' index.html
          python - <<'PY'
          import re
          from pathlib import Path
          s=Path('index.html').read_text(encoding='utf-8')
          scripts=re.findall(r'<script(?:\s[^>]*)?>(.*?)</script>',s,re.S)
          Path('/tmp/finanza-app.js').write_text('\n'.join(scripts),encoding='utf-8')
          PY
          node --check /tmp/finanza-app.js
      - name: Commit updated app
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html
          git diff --cached --quiet || git commit -m 'Aplicar diseño Cocoa V4 y mejoras solicitadas'
          git push